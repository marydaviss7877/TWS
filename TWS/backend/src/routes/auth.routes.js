/**
 * Auth Module Routes
 * Consolidated entry point for all authentication routes.
 * Delegates to the auth module's internal route definitions.
 */
module.exports = require('../modules/auth/routes');
