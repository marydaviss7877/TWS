/**
 * Core Module Routes
 * Consolidated entry point for all core module routes.
 * Delegates to the core module's internal route definitions.
 * Note: core.routes.js is a separate lightweight health/metrics file.
 */
module.exports = require('../modules/core/routes');
