/**
 * Tenant Module Routes
 * Consolidated entry point for all tenant routes.
 * Delegates to the tenant module's internal route definitions.
 */
module.exports = require('../modules/tenant/routes');
